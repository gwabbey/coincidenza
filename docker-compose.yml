networks:
  web:
    external: true

services:
  trasporti:
    container_name: trasporti
    build: .
    restart: always
    ports:
      - "3003:3003"
    networks:
      - web
    dns:
      - 8.8.8.8
      - 8.8.4.4